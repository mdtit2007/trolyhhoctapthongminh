<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Đăng Nhập | Học Thông Minh</title>
    <link rel="stylesheet" type="text/css" href="~/Content/assest/styles/bootstrap4/bootstrap.min.css">

    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="~/Content/clients/plugins/fontawesome-free/css/all.min.css">
    <!-- icheck bootstrap -->
    <link rel="stylesheet" href="~/Content/clients/plugins/icheck-bootstrap/icheck-bootstrap.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/Content/clients/dist/css/adminlte.min.css">

</head>
<body class="hold-transition login-page">
    <div class="login-box">
        @RenderBody()
    </div>

    <!-- jQuery -->
    <script src="~/Content/clients/plugins/jquery/jquery.min.js"></script>
    <!-- Bootstrap 4 -->
    <script src="~/Content/clients/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- AdminLTE App -->
    <script src="~/Content/clients/dist/js/adminlte.min.js"></script>
</body>
</html>
<style>
    /* Ảnh nền sẽ được áp dụng cho toàn bộ trang */
    .login-page {
        background-image: url('@Url.Content("~/Content/assest/img/banner2.png")'); /* Đảm bảo đường dẫn ảnh đúng */
        background-size: cover; /* Làm cho ảnh chiếm toàn bộ màn hình */
        background-position: center; /* Căn giữa ảnh */
        background-repeat: no-repeat; /* Ngừng lặp lại ảnh nếu nó nhỏ hơn màn hình */
        height: 100vh; /* Chiều cao đầy đủ của cửa sổ trình duyệt */
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative; /* Để thêm lớp phủ mờ */
    }

    /* Tạo lớp nền mờ trong suốt */
    .login-page::after {
        content: ''; /* Nội dung rỗng */
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(255, 255, 255, 0.5); /* Nền trắng với độ mờ trong suốt */
        z-index: -1; /* Để nền mờ dưới các thành phần khác */
    }
</style>

